# Exemple (ne contient aucun secret)
# Exportez ces variables avant de lancer uvicorn
ADMIN_TOKEN=dev-token
# Optionnel: forcer le chemin vers public/catalog
# CATALOG_ROOT=/chemin/vers/Medilec/public/catalog
