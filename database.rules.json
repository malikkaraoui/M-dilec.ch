{
  "rules": {
    ".read": false,
    ".write": false,

    "products": {
      ".read": true,
      ".write": "auth != null && auth.token.role === 'admin'",
      ".indexOn": ["name", "brand"],

      "$productId": {
        ".validate": "newData.hasChild('name') && newData.child('name').isString() && newData.child('name').val().length <= 120 && (!newData.child('brand').exists() || (newData.child('brand').isString() && newData.child('brand').val().length <= 80)) && (!newData.child('description').exists() || (newData.child('description').isString() && newData.child('description').val().length <= 4000)) && (!newData.child('priceCents').exists() || newData.child('priceCents').isNumber()) && (!newData.child('pdf').exists() || (( !newData.child('pdf/storagePath').exists() || (newData.child('pdf/storagePath').isString() && newData.child('pdf/storagePath').val().length <= 500) ) && ( !newData.child('pdf/downloadURL').exists() || (newData.child('pdf/downloadURL').isString() && newData.child('pdf/downloadURL').val().length <= 2000) ))) && (!newData.child('image').exists() || (( !newData.child('image/storagePath').exists() || (newData.child('image/storagePath').isString() && newData.child('image/storagePath').val().length <= 500) ) && ( !newData.child('image/downloadURL').exists() || (newData.child('image/downloadURL').isString() && newData.child('image/downloadURL').val().length <= 2000) )))"
      }
    },

    "users": {
      ".read": "auth != null && auth.token.role === 'admin'",
      "$uid": {
        ".read": "auth != null && (auth.token.role === 'admin' || auth.uid === $uid)",
        ".write": "auth != null && (auth.token.role === 'admin' || auth.uid === $uid)",

        "phone": {
          ".validate": "newData.isString() && newData.val().length <= 40"
        },
        "email": {
          ".validate": "newData.val() === null || (newData.isString() && newData.val().length <= 200)"
        },
        "updatedAt": {
          ".validate": "newData.exists()"
        }
      }
    },

    "orders": {
      ".indexOn": ["createdAt", "user/uid"],

      "$orderId": {
        ".read": "auth != null && (auth.token.role === 'admin' || data.child('user/uid').val() === auth.uid)",

        ".write": "auth != null && (auth.token.role === 'admin' || (!data.exists() && newData.child('user/uid').val() === auth.uid))",

        ".validate": "newData.hasChildren(['id','createdAt','status','user','items']) && newData.child('id').val() === $orderId",

        "id": {
          ".validate": "newData.isString() && newData.val() === $orderId"
        },
        "createdAt": {
          ".validate": "newData.isNumber() || (newData.hasChild('.sv') && newData.child('.sv').val() === 'timestamp')"
        },
        "source": {
          ".validate": "!newData.exists() || (newData.isString() && newData.val().length <= 40)"
        },
        "note": {
          ".validate": "newData.val() === null || (newData.isString() && newData.val().length <= 2000)"
        },
        "adminNote": {
          ".validate": "!newData.exists() || (auth != null && auth.token.role === 'admin' && newData.isString() && newData.val().length <= 4000)"
        },
        "status": {
          ".validate": "auth != null && ((auth.token.role === 'admin' && (newData.val() === 'new' || newData.val() === 'processing' || newData.val() === 'done' || newData.val() === 'cancelled')) || (auth.token.role !== 'admin' && newData.val() === 'new'))"
        },
        "updatedAt": {
          ".validate": "!newData.exists() || (auth != null && auth.token.role === 'admin')"
        },
        "paid": {
          ".validate": "auth != null && auth.token.role === 'admin'"
        },

        "user": {
          "uid": {
            ".validate": "newData.isString() && auth != null && (auth.token.role === 'admin' || newData.val() === auth.uid)"
          },
          "email": {
            ".validate": "newData.val() === null || (newData.isString() && newData.val().length <= 200)"
          },
          "phone": {
            ".validate": "newData.isString() && newData.val().length <= 40"
          }
        },

        "items": {
          ".validate": "newData.hasChildren()",
          "$idx": {
            ".validate": "$idx.matches(/^(?:[0-9]|[1-3][0-9]|4[0-9])$/) && newData.hasChildren(['id','qty']) && newData.child('id').isString() && newData.child('id').val().length > 0 && newData.child('id').val().length <= 120 && newData.child('qty').isNumber() && newData.child('qty').val() >= 1 && newData.child('qty').val() <= 999 && (!newData.child('name').exists() || (newData.child('name').isString() && newData.child('name').val().length <= 160)) && (!newData.child('brand').exists() || (newData.child('brand').isString() && newData.child('brand').val().length <= 80)) && (!newData.child('priceCents').exists() || newData.child('priceCents').isNumber())"
          }
        }
      }
    },

    "userOrders": {
      "$uid": {
        ".read": "auth != null && (auth.token.role === 'admin' || auth.uid === $uid)",

        "$orderId": {
          ".write": "auth != null && (auth.token.role === 'admin' || auth.uid === $uid) && newData.val() === true",
          ".validate": "newData.val() === true"
        }
      }
    }
    ,
    "carts": {
      ".read": "auth != null && auth.token.role === 'admin'",

      "$uid": {
        ".read": "auth != null && (auth.token.role === 'admin' || auth.uid === $uid)",
        ".write": "auth != null && (auth.token.role === 'admin' || auth.uid === $uid)",

        ".validate": "newData.hasChildren(['uid','updatedAt','items']) && newData.child('uid').val() === $uid",

        "uid": {
          ".validate": "newData.isString() && newData.val() === $uid"
        },
        "updatedAt": {
          ".validate": "newData.isNumber() || (newData.hasChild('.sv') && newData.child('.sv').val() === 'timestamp')"
        },
        "items": {
          ".validate": "newData.exists()",
          "$productId": {
            ".validate": "newData.hasChildren(['id','qty']) && newData.child('id').isString() && newData.child('id').val() === $productId && newData.child('qty').isNumber() && newData.child('qty').val() >= 1 && newData.child('qty').val() <= 999 && (!newData.child('name').exists() || (newData.child('name').isString() && newData.child('name').val().length <= 160)) && (!newData.child('brand').exists() || (newData.child('brand').isString() && newData.child('brand').val().length <= 80)) && (!newData.child('priceCents').exists() || newData.child('priceCents').isNumber())"
          }
        }
      }
    },

    "guestCarts": {
      ".read": "auth != null && auth.token.role === 'admin'",
      ".write": false,

      "$cartId": {
        ".read": true,
        ".write": true,

        ".validate": "newData.hasChildren(['cartId','updatedAt','items']) && newData.child('cartId').val() === $cartId",

        "cartId": {
          ".validate": "newData.isString() && newData.val() === $cartId"
        },
        "updatedAt": {
          ".validate": "newData.isNumber() || (newData.hasChild('.sv') && newData.child('.sv').val() === 'timestamp')"
        },
        "items": {
          ".validate": "newData.exists()",
          "$productId": {
            ".validate": "newData.hasChildren(['id','qty']) && newData.child('id').isString() && newData.child('id').val() === $productId && newData.child('qty').isNumber() && newData.child('qty').val() >= 1 && newData.child('qty').val() <= 999 && (!newData.child('name').exists() || (newData.child('name').isString() && newData.child('name').val().length <= 160)) && (!newData.child('brand').exists() || (newData.child('brand').isString() && newData.child('brand').val().length <= 80)) && (!newData.child('priceCents').exists() || newData.child('priceCents').isNumber())"
          }
        }
      }
    }
  }
}
